seed: 1701
device: cpu
task:
  name: keyed_parity
  train_samples: 2048
  val_samples: 512
  test_samples: 1024
  seq_len: 65
  train_history_len: 16
  test_history_len: 64
  num_entities: 16
  num_relations: 8
  num_active_keys: 2
  revision_probability: 0.70
model:
  d_model: 32
  memory_rank: 16
  num_banks: 1
  num_layers: 1
  num_heads: 4
  dropout: 0.0
  write_gate_mode: current
  readout_mode: memory_only
  track_spectral_diagnostics: false
train:
  epochs: 30
  batch_size: 64
  learning_rate: 0.001
  weight_decay: 0.0
  grad_clip: 1.0
  amp: false
  num_workers: 0
experiment:
  seeds: [1701]
  gate_modes: [current, safe, expressive]
  readout_modes: [memory_only]
output_dir: results/a1_keyed_parity_dev
